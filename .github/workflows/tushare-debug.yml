name: Tushare调试
on: workflow_dispatch
jobs:
  debug:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@v5
      - run: pip install requests
      - env:
          TUSHARE_TOKEN: ${{ secrets.TUSHARE_TOKEN }}
        run: python tushare_debug.py
